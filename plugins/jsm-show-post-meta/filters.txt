
'jsmspm_metabox_table_metadata' ( array $metadata, $post_obj )		// Filter the post meta shown in the metabox.

'jsmspm_metabox_table_exclude_keys' ( array $exclude_keys, $post_obj )	// Array of regular expressions to exclude meta keys.

'jsmspm_show_metabox_capability' ( 'manage_options', $post_obj )	// Capability required to show post meta.

'jsmspm_show_metabox_post_type' ( true, $post_type )			// Show post meta for a post type (defaults to true).

'jsmspm_delete_meta_capability' ( 'manage_options', $post_obj )		// Capability required to delete post meta.

'jsmspm_delete_meta_icon_class' ( 'dashicons dashicons-table-row-delete' )	// Icon for the delete post meta button.

